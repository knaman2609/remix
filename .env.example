# Get your free Gemini API key at https://aistudio.google.com/apikey
# This enables Lyria RealTime for stem regeneration
# Without it, the app still works for stem separation (Demucs) — swaps return silence placeholders
GEMINI_API_KEY=your_gemini_api_key_here
